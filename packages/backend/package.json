{
    "name": "@real-blackjack/backend",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "vitest run --config vitest.config.ts",
        "test:integration": "vitest run --config vitest-integration.config.ts",
        "dev": "ts-node-dev --respawn --transpile-only src/main.ts"
    },
    "dependencies": {
        "express": "^4.0.0",
        "zod": "^3.0.0"
    },
    "devDependencies": {
        "@types/express": "^4.0.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    }
}
