import type { JSX } from 'react';

import { TableScreen } from '~/screens/TableScreen';

// eslint-disable-next-line import/no-default-export
export default function TableRoute(): JSX.Element {
    return <TableScreen />;
}
